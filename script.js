/* function myFunc(argument) {
  const newObj = JSON.parse(JSON.stringify(obj));
  newObj.key = "valami";

  return argument;
}

const obj = {
  key: "kismacska",
};

const myArrowFunc = (argument) => argument;

console.log(myFunc(obj));
console.log(myArrowFunc(obj)); */

// const manipulateText = (string) => {
//   if (typeof sting !== "string") {
//     return "please enter a string";
//   } else {
//     return "ok";
//   }
// };

// console.log(manipulateText(null));

const manipulateText = (string) => {};
